test object generation with different params

automatic camera placement (random location in area), same with light

random object generation with tolerances?

test different voxel sizes
detect GPU for cycles render
improve metal, (wood shaders, shiny shaders for eevee, rough shader for eevee)